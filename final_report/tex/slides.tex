% Final presentation slides — 5 min / 7 frames
% Geometrically Inductive SSL for EEG  (v2 regime: cross-montage transfer)
% =============================================================================

\documentclass[aspectratio=169]{beamer}

\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{xcolor}

\usetikzlibrary{positioning, arrows.meta, calc}

\usetheme{metropolis}
% Fallback for environments without metropolis: comment the line above and
% uncomment the two below.
% \usetheme{default}
% \usecolortheme{whale}

% Common colors
\definecolor{geocolor}{HTML}{2E86AB}
\definecolor{posonlycolor}{HTML}{5DA9C9}
\definecolor{codexcolor}{HTML}{A23B72}
\definecolor{chindcolor}{HTML}{888888}
\definecolor{winnercolor}{HTML}{18A558}
\definecolor{accentred}{HTML}{D7263D}

% Title meta
\title{Geometrically Inductive Self-Supervised Learning for EEG}
\subtitle{A Design Study of Spatial Attention Across Sessions, Subjects, and Montages}
\author{Tianxin Zhou}
\institute{ECEN 525 -- Brain--Computer Interaction \\ Santa Clara University}
\date{Spring 2026}

\begin{document}

% =============================================================================
% Slide 1 — Title
% =============================================================================
\begin{frame}[plain]
  \titlepage
  \vfill
  \centering
  \footnotesize
  \texttt{github.com/tianxin-scu/geometric-eeg-ssl}
\end{frame}

% =============================================================================
% Slide 2 — Problem statement
% =============================================================================
\begin{frame}{Problem: EEG is non-stationary -- and the geometry is what changes}
  \vspace{-0.4em}
  \begin{columns}[T]
    \begin{column}{0.42\textwidth}
      \small
      EEG varies on \textbf{three geometric axes}:
      \begin{itemize}
        \setlength{\itemsep}{2pt}
        \item \textbf{Session} — cap shifts day to day. \emph{(motivates)}
        \item \textbf{Subject} — head sizes differ. \emph{(motivates)}
        \item \textbf{Montage} — 2 to 256 electrodes in different layouts. \textbf{$\leftarrow$ what I test}
      \end{itemize}
      \vspace{0.4em}
      \footnotesize
      \textcolor{codexcolor}{\textbf{Codex models}} (LaBraM, EEGPT):
      identity-indexed embeddings --- \textbf{cannot even run} on an unseen layout.
      \textcolor{chindcolor}{\textbf{Channel-independent}} (BIOT):
      no spatial structure at all.
      \\[0.3em]
      Neither uses scalp geometry as an \emph{input}.
    \end{column}
    \begin{column}{0.54\textwidth}
      \centering
      % --- Montage figure ---
      \begin{tikzpicture}[scale=0.78, every node/.style={font=\scriptsize}]
        % Dense: 64-ch (train)
        \draw[thick] (0,0) circle (1.1);
        \foreach \a in {0, 30, 60, 90, 120, 150, 180, 210, 240, 270, 300, 330} {
          \fill[geocolor] ({0.85*cos(\a)}, {0.85*sin(\a)}) circle (0.07);
          \fill[geocolor] ({0.50*cos(\a+15)}, {0.50*sin(\a+15)}) circle (0.055);
        }
        \node[below] at (0,-1.35) {\textbf{train:} 64 ch};
        % Arrow
        \draw[->, very thick, gray] (1.4,0) -- (2.2,0);
        % Sparse: 3-ch (test)
        \draw[thick] (3.6,0) circle (1.1);
        \fill[accentred] (2.9, 0) circle (0.11);
        \fill[accentred] (3.6, 0) circle (0.11);
        \fill[accentred] (4.3, 0) circle (0.11);
        \node[below] at (3.6,-1.35) {\textbf{test:} 3 ch (unseen)};
      \end{tikzpicture}

      \vspace{0.3em}

      % --- Flow graph ---
      \begin{tikzpicture}[
          >=Stealth,
          every node/.style={font=\scriptsize},
          box/.style={draw, rounded corners, minimum height=0.58cm, align=center, inner sep=3pt},
          scale=0.84, transform shape,
        ]
        % Raw EEG input (shared by both paths)
        \node[box, fill=gray!10, minimum width=2.2cm] (eeg) at (0.45, 3.4)
          {EEG signal\\$(B,C,T)$};
        % Patch projection (shared)
        \node[box, fill=gray!16, minimum width=2.2cm] (proj) at (0.45, 2.0)
          {patch proj\\$(B,C,D)$};
        % Ch idx lookup (codex path only)
        \node[box, fill=codexcolor!12, minimum width=2.2cm] (idx) at (3.0, 3.4)
          {Ch.\ idx look-up\\$(C,D)$};
        % Plus node (codex path only)
        \node[draw, circle, fill=codexcolor!20, minimum size=0.50cm, inner sep=1pt]
          (plus) at (3.0, 2.0) {$+$};
        % Model block
        \node[box, fill=gray!18, minimum width=2.0cm] (model) at (1.65, 0.3)
          {Model};
        % Output
        \node (out) at (1.65, -0.65) {output};
        % Shared: EEG → proj
        \draw[->, semithick] (eeg.south) -- (proj.north);
        % ChInd: proj → Model directly (gray)
        \draw[->, thick, chindcolor]
          (proj.south) -- (model.north);
        % Codex: proj → + , idx → + → Model (purple)
        \draw[->, thick, codexcolor]
          (proj.east) to[out=0, in=180] (plus);
        \draw[->, thick, codexcolor]
          (idx.south) -- (plus.north);
        \draw[->, thick, codexcolor]
          (plus.south) -- (model.north);
        % Output arrow
        \draw[->, semithick] (model.south) -- (out.north);
      \end{tikzpicture}
    \end{column}
  \end{columns}
\end{frame}

% =============================================================================
% Slide 3 — Solution: geometry-conditioned attention
% =============================================================================
\begin{frame}{Solution: spatial attention conditioned on $g_{ij}$}
  \vspace{-0.3em}
  \begin{columns}[T]
    \begin{column}{0.42\textwidth}
      \small
      \[
        g_{ij} = \bigl[\,p_i,\;\; p_j,\;\; p_i - p_j,\;\; \|p_i - p_j\|\,\bigr] \in \mathbb{R}^{10}
      \]
      \vspace{0.1em}
      \footnotesize
      \textbf{Example:} $g_{\texttt{C3}\to\texttt{Cz}}$ (normalized coords)
      \\[0.3em]
      \renewcommand{\arraystretch}{1.25}
      \begin{tabular}{@{}llr@{}}
        \toprule
        $p_i$ (\texttt{C3})    & $(x,y,z)$ & $(-0.71,\,0.00,\,0.59)$ \\
        $p_j$ (\texttt{Cz})    & $(x,y,z)$ & $(\phantom{-}0.00,\,0.00,\,0.95)$ \\
        $p_i - p_j$            & disp.     & $(-0.71,\,0.00,\,-0.36)$ \\
        $\|p_i - p_j\|$        & dist.     & $0.80$ \\
        \bottomrule
      \end{tabular}
      \\[0.4em]
      \scriptsize $3+3+3+1 = 10$ dims. \textbf{pos\_only} ablation keeps just $[p_i,p_j]\in\mathbb{R}^6$.
    \end{column}
    \begin{column}{0.56\textwidth}
      \centering
      \begin{tikzpicture}[
          >=Stealth,
          every node/.style={font=\scriptsize},
          box/.style={draw, rounded corners, minimum height=0.5cm, minimum width=1.1cm, align=center, inner sep=3pt},
          op/.style={draw, circle, fill=geocolor!18, minimum size=0.5cm, inner sep=0pt},
          mlp/.style={draw, rounded corners, fill=geocolor!25, minimum height=0.5cm, inner sep=3pt},
          scale=1.0, transform shape,
        ]
        % --- main attention chain (x = 0) ---
        \node[box, fill=gray!10]        (patch) at (0, 4.4) {patch};
        \node[box, fill=geocolor!12]    (qkv)   at (0, 3.5) {$q,\,k,\,v$};
        \node[box]                      (att)   at (0, 2.6) {dot-prod};
        \node[op]                       (plus)  at (0, 1.8) {$+$};
        \node[box]                      (soft)  at (0, 1.0) {softmax};
        \node[op]                       (dot)   at (0, 0.0) {$\cdot$};
        \node[box, fill=gray!10]        (out)   at (0,-0.9) {output};

        % --- geometry side (right) ---
        \node[box, fill=geocolor!12]    (gij)   at (2.7, 3.95) {$g_{ij}$};
        \node[mlp]                      (bmlp)  at (2.7, 2.6)  {bias MLP};
        \node[mlp]                      (wmlp)  at (2.7, 1.0)  {value MLP};
        \node[op]                       (vsum)  at (1.35, 0.0) {$+$};

        % --- main chain arrows ---
        \draw[->] (patch) -- (qkv);
        \draw[->] (qkv) -- (att) node[midway, right, font=\tiny] {$q,k$};
        \draw[->] (att) -- (plus);
        \draw[->] (plus) -- (soft);
        \draw[->] (soft) -- (dot) node[midway, left, font=\tiny] {$a_{ij}$};
        \draw[->] (vsum) -- (dot);
        \draw[->] (dot) -- (out);

        % --- geometry: g_ij feeds BOTH MLPs (parallel) ---
        \draw[->, geocolor] (gij) -- (bmlp);
        \draw[->, geocolor] (gij.east) to[out=-90, in=90] (wmlp.east);
        \draw[->, geocolor] (bmlp) -- (plus) node[midway, above, font=\tiny] {$b(g_{ij})$};
        \draw[->, geocolor] (wmlp) -- (vsum) node[midway, above, font=\tiny] {$w(g_{ij})$};
        % v from the q,k,v box into the value sum
        \draw[->] (qkv.east) to[out=0, in=90] (1.35, 0.25) -- (vsum)
          node[pos=0.18, above, font=\tiny] {$v$};
      \end{tikzpicture}
    \end{column}
  \end{columns}
\end{frame}

% =============================================================================
% Slide 4 — Headline: scale makes the geometric advantage significant
% =============================================================================
\begin{frame}{Headline: at scale, geometric beats chind on zero-shot transfer}
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      width=10cm, height=6.2cm,
      ybar, bar width=42pt,
      enlarge x limits=0.28,
      ymin=0.49, ymax=0.56,
      ylabel={Zero-shot BAC (BCIC-2B)},
      symbolic x coords={chind, pos\_only, geom},
      xtick=data,
      nodes near coords,
      nodes near coords style={font=\small},
      tick label style={font=\normalsize},
      label style={font=\normalsize},
    ]
      \addplot[fill=chindcolor!70,   bar shift=0pt] coordinates {(chind,0.509)};
      \addplot[fill=posonlycolor!85, bar shift=0pt] coordinates {(pos\_only,0.531)};
      \addplot[fill=geocolor!85,     bar shift=0pt] coordinates {(geom,0.545)};
    \end{axis}
  \end{tikzpicture}
  \\[0.4em]
  78 PhysioNet $+$ 78 Sleep-EDFx pretrain \quad$\to$\quad zero-shot to BCIC-2B
\end{frame}

% =============================================================================
% Slide 5 — Conclusion + future work
% =============================================================================
\begin{frame}{Conclusion: a clean positive, with clear edges}
  \begin{columns}[T]
    \begin{column}{0.9\textwidth}
      \small
      \textbf{What I found:}
      \begin{itemize}
        \setlength{\itemsep}{3pt}
        \item Geometry-conditioned attention \textbf{transfers zero-shot} to an unseen montage and \textbf{beats chind significantly} once given enough data.
        \item Robust to descriptor reduction (pos\_only also significant).
      \end{itemize}
      \vspace{0.5em}
      \textbf{Honest limits:}
      \begin{itemize}
        \setlength{\itemsep}{3pt}
        \item Significant on \textbf{one} held-out montage (BCIC); small-corpus tests directional but underpowered (9-subject ceiling).
      \end{itemize}
      \vspace{0.5em}
      \textbf{Future work:} second distinct-montage held-out (e.g.\ 128\,ch); larger corpus; implement the codex baseline.
    \end{column}
  \end{columns}
\end{frame}

% =============================================================================
% Slide 6 — Predicted vs. observed scorecard
% =============================================================================
\begin{frame}{Did the results match the proposal's predictions?}
  \vspace{0.5em}
  \centering
  \textbf{Predicted vs.\ observed}\\[0.8em]
  \begin{tabular}{lcc}
    \toprule
                      & \textbf{Predicted} & \textbf{Observed} \\
    \midrule
    Direction (3 montages) & geom $+$  & \textcolor{winnercolor}{\textbf{geom $+$ (3/3)}} \\[0.3em]
    Powered transfer       & geom $++$ & \textcolor{winnercolor}{\textbf{geom $++$, sig.}} \\[0.3em]
    Across all montages    & hoped     & 1 powered; rest future \\
    \bottomrule
  \end{tabular}
  \\[1.5em]
  \footnotesize
  \texttt{github.com/tianxin-scu/geometric-eeg-ssl}
\end{frame}

% =============================================================================
% Slide 7 — Key references
% =============================================================================
\begin{frame}{Key References}
  \footnotesize
  \begin{itemize}
    \setlength{\itemsep}{3pt}
    \item Wang et al. \textbf{EEGPT}. NeurIPS 2024 -- training recipe (momentum + reconstruction).
    \item Grill et al. \textbf{BYOL}. NeurIPS 2020 -- momentum-encoder alignment.
    \item He et al. \textbf{MAE}. CVPR 2022 -- masked-patch reconstruction.
    \item Hamilton, Ying, Leskovec. \textbf{GraphSAGE}. NeurIPS 2017 -- inductive vs. transductive vocabulary.
    \item Veli\v{c}kovi\'c et al. \textbf{GAT}. ICLR 2018 -- attention over relational features.
    \item Jiang et al. \textbf{LaBraM}. ICLR 2024; Yang et al. \textbf{BIOT}. NeurIPS 2023 -- codex / channel-independent baselines.
    \item Schalk et al. 2004 -- PhysioNet MI; Tangermann et al. 2012 -- BCIC-2B; Kemp et al. 2000 -- Sleep-EDF.
  \end{itemize}
  \vspace{0.6em}
  \centering
  \scriptsize
  Full reference list and per-experiment details in the project report.
\end{frame}

\end{document}
