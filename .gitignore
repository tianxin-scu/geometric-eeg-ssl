# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Data and checkpoints (NEVER committed). Anchored to repo root so that
# tracked config dirs like configs/data/ are not swept up.
/data/
/datasets/
/runs/
/cache/
/mne_data/
/checkpoints/
*.edf
*.gdf
*.fif
*.set
*.pt
*.ckpt
*.pth
*.npy
*.npz

# Experiment tracking
wandb/
mlruns/
.hydra/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# LaTeX. Sources stay local-only; only the compiled PDFs are published.
*.tex
*.bib
*.cls
*.sty
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.fls
*.fdb_latexmk
*.nav
*.snm
*.synctex.gz
*.xdv

# docs/ is entirely local: internal notes, project summary, LaTeX sources.
# GitHub publishes only the compiled PDFs in report/ and the reproduction code.
/docs/*

# Claude Code / assistant context — never committed
CLAUDE.md
.claude/

# External reference repos (not part of this project)
EEGPT/
