# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Data and checkpoints (NEVER committed). Anchored to repo root so that
# tracked config dirs like configs/data/ are not swept up.
/data/
/datasets/
/runs/
/cache/
/mne_data/
/checkpoints/
*.edf
*.gdf
*.fif
*.set
*.pt
*.ckpt
*.pth
*.npy
*.npz

# Experiment tracking
wandb/
mlruns/
.hydra/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# LaTeX. Sources stay local-only; only the compiled PDFs are published.
*.tex
*.bib
*.cls
*.sty
*.aux
*.log
*.out
*.bbl
*.blg
*.toc
*.fls
*.fdb_latexmk
*.nav
*.snm
*.synctex.gz
*.xdv

# docs/: only the public-facing summary is tracked; anything else stays local.
/docs/*
!/docs/project_summary.md

# Claude Code / assistant context — never committed
CLAUDE.md
.claude/

# External reference repos (not part of this project)
EEGPT/
